mutation releaseFulfillmentHold($fulfillmentOrderId: ID!) {
  fulfillmentOrderReleaseHold(id: $fulfillmentOrderId) {
    fulfillmentOrder {
      id
      status
    }
    userErrors {
      field
      message
    }
  }
}
