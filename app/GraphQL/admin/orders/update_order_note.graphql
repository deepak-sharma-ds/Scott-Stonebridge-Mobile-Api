mutation updateOrderNote($id: ID!, $note: String!) {
  orderUpdate(input: {
    id: $id
    note: $note
  }) {
    order {
      id
      note
    }
    userErrors {
      field
      message
    }
  }
}
